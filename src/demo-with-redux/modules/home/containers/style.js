import {StyleSheet} from "react-native"

export default StyleSheet.create({
  view: {
    borderWidth: 4,
    borderColor: 'blue',
  },
  scrollView: {
    borderWidth: 4,
    borderColor: 'red',
    flex: 1,
  },
})